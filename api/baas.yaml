# Dropwizard configuration

metadataPath: derived_metadata.json
mediaBaseUrl: https://an.example.domain/and/path/with/separator/

server:
  applicationConnectors:
    - type: http
      port: 8080

  adminConnectors:
    - type: http
      port: 8081


logging:
  level: INFO

  loggers:
    io.bunnies.baas: DEBUG

  appenders:
    - type: console

jedis:
  endpoint: localhost:6379