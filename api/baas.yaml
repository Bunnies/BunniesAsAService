# Dropwizard configuration

derivedMetadataPath: derived_metadata.json
specifiedMetadataPath: specified_metadata.json
mediaBaseUrl: https://an.example.domain/and/path/with/separator/

server:
  applicationConnectors:
    - type: http
      port: 8100

  adminConnectors:
    - type: http
      port: 8101


logging:
  level: INFO

  loggers:
    io.bunnies.baas: DEBUG

  appenders:
    - type: console

jedis:
  endpoint: localhost:6379
